# Deslop Public Surfaces Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove low-value, repetitive, stale, or generic public content from Bilig while keeping runnable WorkPaper proof, current limits, and adoption paths easy to find.

**Architecture:** Treat slop as a content-quality bug: every public entry point must have one job, one current proof path, and one clear boundary. Edit canonical Markdown first, regenerate derived discovery files through repo scripts, and validate with docs checks before committing.

**Tech Stack:** Markdown, TypeScript discovery checks, `pnpm`, GitHub CLI, npm registry readback.

---

## Cleanup Standard

Use these external anchors for the working definition:

- AI slop is low-quality AI-generated content, usually produced at scale without enough useful substance: <https://en.wikipedia.org/wiki/AI_slop>.
- Google treats scaled low-value content as spam whether generated by humans, automation, or a mix: <https://developers.google.com/search/docs/essentials/spam-policies#scaled-content-abuse>.
- Helpful content should be made for readers first and should demonstrate first-hand usefulness: <https://developers.google.com/search/docs/fundamentals/creating-helpful-content>.

For this repo, delete or rewrite content when it is:

- repeated in multiple entry points without adding a new decision;
- stale against `origin/main`, npm latest, GitHub issue state, or live package proofs;
- XLSX-first when the page is supposed to explain WorkPaper runtime proof;
- a directory of links where a reader needs one next command;
- a broad claim without a runnable evaluator, benchmark artifact, limitation, or issue link.

Do not remove:

- runnable evaluator commands;
- compatibility limits;
- generated discovery files that are regenerated by scripts;
- saved-file docs when the page is explicitly about import/export or XLSX recalculation.

## File Structure

- Modify: `README.md`
  - Make the root landing page less repetitive by keeping one first proof and one routing table before deeper docs.
- Modify: `packages/workpaper/README.md`
  - Keep the npm first screen WorkPaper-first, shorten command dumps, and move saved-file commands into their own boundary section.
- Modify: `docs/agent-adoption-kit.md`
  - Reduce host-install repetition; keep the proof bar and maintainer/outreach guard.
- Modify: `docs/headless-workpaper-agent-handbook.md`
  - Keep one copy-paste prompt and one MCP setup path; remove duplicate host lists covered by the rule chooser.
- Modify: `docs/agent-start.txt`
  - Keep the compact agent path under one screen where possible.
- Modify: `internal/growth/community-growth-snapshot.md`
  - Refresh through `pnpm community:growth:snapshot:markdown`, not by hand.
- Modify as needed: `scripts/check-docs-discovery*.ts`
  - Update only brittle phrase checks that force duplicate prose.
- Regenerate if canonical docs change: `docs/llms-full.txt` and `docs/.well-known/llms-full.txt` through `pnpm tsx scripts/sync-agent-discovery-docs.ts`.

### Task 1: Lock The Current Public Truth

- [x] **Step 1: Verify repo and live public state**

Run:

```bash
git status --short --branch
git rev-parse HEAD origin/main
gh api repos/proompteng/bilig --jq '{stars:.stargazers_count,forks:.forks_count,watchers:.subscribers_count,pushedAt:.pushed_at}'
gh issue list --repo proompteng/bilig --state open --limit 20
gh pr list --repo proompteng/bilig --state open --limit 20
npm view @bilig/workpaper version
```

Expected: clean checkout before editing, current GitHub star count recorded, and npm latest used in any refreshed snapshot.

- [x] **Step 2: Refresh the growth snapshot**

Run:

```bash
pnpm community:growth:snapshot:markdown
```

Expected: `internal/growth/community-growth-snapshot.md` reports current stars, npm version, issue count, PR count, and traffic fields or an explicit unavailable reason.

### Task 2: Clean The NPM WorkPaper First Screen

- [x] **Step 1: Rewrite `packages/workpaper/README.md` around one first proof**

Keep:

```markdown
npm exec --yes --package @bilig/workpaper@latest -- bilig-evaluate --door workpaper-service --json
```

Then keep one MCP evaluator and one `bilig-agent-start --json` router. Move `--from-xlsx` and workflow-server commands out of the first screen.

- [x] **Step 2: Check the README no longer has an undifferentiated command dump**

Run:

```bash
rg -n 'bilig-n8n-formula-server|--from-xlsx' packages/workpaper/README.md
```

Expected: those commands appear only in workflow or saved-file boundary sections, not in `## Verify Without Cloning`.

### Task 3: Collapse Root README Duplication

- [x] **Step 1: Keep one first proof block**

Modify `README.md` so the top section and `## If You Only Try One Thing` do not both carry the same full JSON sample. Keep the top sample; turn the later section into routing and trust boundaries.

- [x] **Step 2: Keep WorkPaper first and saved files second**

Search:

```bash
rg -n 'XLSX|xlsx|Excel|spreadsheet UI|file diagnostics' README.md
```

Expected: saved-file language appears only in trust boundaries, imported-file routing, or compatibility sections.

### Task 4: Trim Agent Handoff Repetition

- [x] **Step 1: Shorten `docs/agent-adoption-kit.md`**

Keep the proof table, no-key evaluator, manifest gate, and upstream maintainer notes. Replace long host lists with links to `docs/agent-rule-chooser.md` unless a host needs unique instructions on that page.

- [x] **Step 2: Shorten `docs/headless-workpaper-agent-handbook.md`**

Keep one copy-paste prompt, one MCP config, one output contract, and boundaries. Remove duplicate install lists already covered by the adoption kit or rule chooser.

### Task 5: Regenerate And Validate

- [x] **Step 1: Regenerate discovery files**

Run:

```bash
pnpm tsx scripts/sync-agent-discovery-docs.ts
```

- [x] **Step 2: Run targeted checks**

Run:

```bash
pnpm docs:discovery:check
pnpm exec oxfmt --check packages/workpaper/README.md README.md docs/agent-adoption-kit.md docs/headless-workpaper-agent-handbook.md
git diff --check
```

Expected: all pass.

- [ ] **Step 3: Commit one clean tranche**

Run:

```bash
git add README.md packages/workpaper/README.md docs/agent-adoption-kit.md docs/headless-workpaper-agent-handbook.md docs/agent-start.txt docs/llms-full.txt docs/.well-known/llms-full.txt internal/growth/community-growth-snapshot.md scripts/check-docs-discovery*.ts docs/superpowers/plans/2026-06-25-deslop-public-surfaces.md
git commit -m "docs(discovery): deslop public workpaper surfaces"
git push origin HEAD:main
```

Expected: push succeeds, then GitHub mirror reflects the new commit and checks start.
