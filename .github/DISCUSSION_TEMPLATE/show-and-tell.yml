title: "[show] "
body:
  - type: markdown
    attributes:
      value: |
        Share a workflow, prototype, benchmark, example, article, or integration built with `bilig`.
        If you are extending the current runnable example set, link the closest starting point from https://proompteng.github.io/bilig/workbook-automation-examples-node.html.
  - type: input
    id: link
    attributes:
      label: Link
      description: Repository, article, demo, gist, evaluator output, fixture, or package link.
      placeholder: https://github.com/...
    validations:
      required: true
  - type: dropdown
    id: workflow
    attributes:
      label: Workflow
      options:
        - Node service
        - coding agent
        - formula evaluation
        - WorkPaper persistence
        - benchmark
        - import / export
        - browser workbook
        - docs / article
        - other
    validations:
      required: true
  - type: textarea
    id: summary
    attributes:
      label: What did you build?
      description: Keep it concrete. Mention inputs, outputs, package versions, and what worked or failed.
      placeholder: |
        I used ...
        It reads ...
        It writes ...
        The result was ...
    validations:
      required: true
  - type: textarea
    id: feedback
    attributes:
      label: Feedback for bilig
      description: What should be easier, faster, better documented, or more compatible?
